<!-- <h2>Your Pragmatic Cart</h2>
<ul>
  <% cart.line_items.each do |item| %>
    <li> <%= item.quantity %> &times; <%= item.product.title %> </li>
  <% end %>
</ul> -->

<h2>Your Cart</h2>
<table>
<!--
  <% cart.line_items.each do |item| %>
    <tr>
      <td> <%= item.quantity %> &times; </td>
      <td> <%= item.product.title %> </td>
      <td class="item_price"> <%= number_to_currency(item.price) %></td>
      <td> <%= button_to 'Remove item', item, class: 'delete', method: :delete, data: { confirm: 'Are You sure?' } %></td>
    </tr>
  <% end %>
-->
  <%= render(cart.line_items) %>
  <tr class="total_line">
    <td colspan="2">Total</td>
    <td class="total_cell"> <%= number_to_currency(cart.total_price) %></td>
  </tr>
</table>

<div class="checkout">
  <%= button_to 'Checkout', new_order_path, method: :get %>
</div>
<div class="delete">
<%= button_to 'Empty cart', cart, class: 'delete', method: :delete, data: { confirm: 'Are You sure?'} %>
</div>
